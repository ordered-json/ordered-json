[package]
name = "ordered-json"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
description = "Strict JSON preserving document order, duplicate keys, and number literals"
readme = "README.md"

[lib]
path = "src/lib.rs"
